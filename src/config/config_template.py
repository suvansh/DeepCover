from utils import get_repo_dir
from os.path import join

# NOTE copy this file, update the below variables with the correct paths
user = 'my_name'
data_dir = join(get_repo_dir(), 'data')  # path to your data directory
